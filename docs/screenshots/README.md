# Screenshots

Real screenshots of the running app, referenced by the root `README.md`:

- `home-hero.jpg` — the homepage hero
- `destination-grid.jpg` — the "Where to next?" destination hub
- `destination-detail.jpg` — a destination detail page (Tokyo)
- `planner-result.jpg` — the planner with a generated itinerary

To refresh these after a visual change, replace the file with the same name — the
root README already points at these exact paths.
